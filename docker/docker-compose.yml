services:
  mongo:
    image: mongo:8

    container_name: mongodb

    ports:
      - "27007:27017"

    volumes:
      - mongo_data:/data/db


    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD}

    restart: always

    networks:
      - backend

  pgvector:
    image: pgvector/pgvector:0.8.6-pg18-trixie
    container_name: pgvector
    ports:
      - "5432:5432"
    volumes:
      - pgvector_data:/var/lib/postgresq
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    networks:
      - backend
    restart: always


networks:
  backend:

volumes:
  mongo_data:
  pgvector_data: